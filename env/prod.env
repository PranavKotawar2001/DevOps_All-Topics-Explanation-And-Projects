# Template — production values live in AWS Secrets Manager / Parameter Store
AWS_REGION=us-east-1
ENVIRONMENT=prod
PROJECT_NAME=rivermarket
PORT=3000
# DATABASE_URL injected by ECS from Secrets Manager at runtime
