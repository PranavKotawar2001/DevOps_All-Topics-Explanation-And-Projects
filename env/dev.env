# Non-secret defaults for local/dev tooling — never commit real secrets
AWS_REGION=us-east-1
ENVIRONMENT=dev
PROJECT_NAME=rivermarket
PORT=3000
# Local only: use docker-compose or port-forward; production uses Secrets Manager
DATABASE_URL=postgresql://riverapp:localonly@localhost:5432/rivermarket
